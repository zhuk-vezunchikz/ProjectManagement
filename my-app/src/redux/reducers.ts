import initialState from "./initialState";


export function reducers(state = initialState, action: object) {
    switch ({}) {
        default:
            return state;
    }
}

export default reducers;
